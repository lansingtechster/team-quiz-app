{
  "name": "team-quiz-app",
  "version": "1.0.0",
  "description": "Animal Adventure Quiz — AI Builder Series Workshop 4 team project",
  "private": true,
  "scripts": {
    "start": "http-server -p 8080 -c-1",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"tests/unit/*.test.js\"",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "http-server": "^14.1.1"
  }
}
